# Performance CI Validation

The runnable performance example is validated by the repository CI matrix with formatting, static analysis, and tests.

CI should validate committed formatting without relying on generated or temporary validation artifacts.
