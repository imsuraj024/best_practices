# Architecture Examples

Runnable examples demonstrate boundaries with the smallest amount of code necessary.

## Runnable

- [`clean-architecture-app/`](clean-architecture-app/) — presentation → use case → repository boundary

## Reference structures

- `feature-first/` — feature-oriented project structure
- `clean-architecture/` — domain and infrastructure boundaries
- `dependency-injection/` — constructor injection and test replacement

Run the Flutter example with `flutter pub get`, `flutter test`, and `flutter run` from its directory.