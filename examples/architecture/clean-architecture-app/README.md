# Clean Architecture App

A minimal runnable example showing a presentation layer consuming a use case through a repository boundary.

## Run

```bash
flutter pub get
flutter test
flutter run
```

The example intentionally uses an in-memory data source so it has no external dependencies or credentials.
