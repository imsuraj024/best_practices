# Feature-First Example

A minimal project layout showing business capability boundaries.

```text
lib/
├── app/
├── core/
└── features/
    └── profile/
        ├── presentation/
        ├── application/
        ├── domain/
        └── data/
```

Keep the example focused on dependency direction and ownership. Add executable Flutter code when a package-level example is introduced.