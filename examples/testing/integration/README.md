# Integration Test Example

Keep integration tests focused on critical user journeys.

```text
Launch
  ↓
Sign in
  ↓
Load account
  ↓
Perform critical action
  ↓
Verify user-visible result
```

Use controlled test data and environments. Avoid making the suite depend on unstable third-party systems.