# Testing Examples

Examples should demonstrate behavior-focused tests at the appropriate level.

## Planned examples

- `unit/` — deterministic business rules
- `widget/` — user-visible UI behavior
- `integration/` — critical end-to-end journeys
- `golden/` — stable visual contracts where visual regression matters

Every example should explain what behavior it proves and why that test level was selected.
